<Window x:Class="my_school.MVVM.View.Home.HomeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:my_school.MVVM.View.Home"
        mc:Ignorable="d"
        Title="HomeView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid 
            Grid.Row="0" 
            Background="Red" 
            Height="40"
            VerticalAlignment="Top"
            >
            <!-- Adicionei um TextBox como conteúdo do Grid -->
            <TextBlock 
                Text="Gerenciamento..." 
                VerticalAlignment="Center" 
                TextAlignment="Center"
                />
        </Grid>
        <UserControl 
            Grid.Row="1" x:Name="MainContent"/>
    </Grid>
</Window>